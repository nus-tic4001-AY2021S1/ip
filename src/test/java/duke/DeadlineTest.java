package duke;

import duke.tasks.Deadline;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;


public class DeadlineTest {
    @Test
    public void deadline() {
        /**
        Deadline deadline1 = new Deadline("borrow book", "2020-09-10");
        Deadline deadline2 = new Deadline("read book", "2020-09-20");
        Deadline deadline3 = new Deadline("return book", "2020-09-30");

        assertEquals("[D][✘] borrow book (by: Sep 10 2020)", deadline1.toString());
        assertEquals("[D][✘] read book (by: Sep 20 2020)", deadline2.toString());
        assertEquals("[D][✘] return book (by: Sep 30 2020)", deadline3.toString());
         */
    }
}
